<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_order_view_form_print_receipt" model="ir.ui.view">
        <field name="name">sale.order.form.print.receipt</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="payment_term_id" position="after">
                <field name="print_receipt_url" string="Imprimir recibo" widget="url" text="Imprimir ticket" readonly="1"/>
            </field>
        </field>
    </record>

</odoo>